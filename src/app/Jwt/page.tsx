"use client";

import React from "react";
import CoursePageLayout from "@/components/CoursePageLayout";

export default function JwtPage() {
  return (
    <CoursePageLayout
      courseId="jwt"
      courseTitle="JSON Web Tokens (JWT)"
      badgeText="Complete JSON Web Tokens (JWT) Track"
    />
  );
}
